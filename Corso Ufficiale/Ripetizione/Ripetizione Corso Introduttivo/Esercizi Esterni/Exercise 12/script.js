/*Scrivi un codice che prenda il prezzo originale di un prodotto e lo sconto 
applicato come input e calcoli il prezzo scontato utilizzando l'operatore ternario.*/

const fullPrice = 80;
const rebateRate = 20;
const reducedPrice = fullPrice - (fullPrice * rebateRate) / 100;


console.log(reducedPrice)