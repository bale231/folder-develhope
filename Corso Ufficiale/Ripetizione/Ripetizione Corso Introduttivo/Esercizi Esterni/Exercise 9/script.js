/*Scrivi un codice che prenda un numero 
come input e verifichi se è positivo o negativo utilizzando l'operatore ternario.*/

const number = -1;
const result = number >= 0 ? 'Positive' : 'Negative';
console.log(`The result is: ${result}`)